import regex
import os
import  re
import custom_requests
if __name__ == '__main__':
    # regex.get_xlsx_file('rules.xlsx', regex.get_list_of_successful_regex('rules', 'rules.xlsx'))
    custom_requests.main()
